## Catalog service

- HTTP API for events and tickets.
- Backed by Postgres + Redis cache.
- Exposes OpenAPI UI at `/swagger-ui.html`.
- See `services/catalog/src/main/resources/application.yml` for ports and DB config.

